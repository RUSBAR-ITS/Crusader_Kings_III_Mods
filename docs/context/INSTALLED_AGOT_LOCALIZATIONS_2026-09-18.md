# Подключение локальных модов AGOT — 18.09.2026

По запросу пользователя подключены два подготовленных мода: `AGOT_RUS_CORRECT` и `AGOT_Submod_Core_RUS`.

В пользовательский каталог `C:/Users/RUSBAR/Documents/Paradox Interactive/Crusader Kings III/mod/` скопированы файлы регистрации `AGOT_RUS_CORRECT.mod` и `AGOT_SUBMOD_CORE_RUS.mod`. Они указывают на папки в `AGOT_Submods` этого репозитория, поэтому следующие изменения локализации будут доступны игре при новом запуске без повторного копирования содержимого модов.

Оба мода зарегистрированы как локальные в `launcher-v2.sqlite`, включены в активный набор **AGOT-RUSBAR**, а порядок загрузки согласован с `dlc_load.json`:

1. A Game of Thrones
2. A Game of Thrones | Русификатор
3. AGOT Submod Core
4. AGOT+
5. AGOT+ RUS
6. Clear Notifications
7. AGOT | Исправления русификатора
8. AGOT Submod Core | Русификатор

Предыдущие шесть записей сохранили свой порядок и состояние. Другой игровой набор `RUSBAR` сравнен с резервной копией и не изменён. Настройки отключённых DLC сохранены.

Перед изменениями сохранены `launcher-v2.sqlite` и `dlc_load.json` в:

```text
C:/Users/RUSBAR/Documents/Paradox Interactive/Crusader Kings III/mod_backups/AGOT_localizations_20260918_062036_77a0f09e/
```

Установка сначала выполнена на копии базы. Проверены повторное выполнение без дублей, сохранение другого набора, целостность SQLite и внешних ключей. После применения к игре повторно проверены оба файла `.mod`, восемь включённых модов и соответствие порядка в базе и JSON. Совпадений относительных путей полных файлов локализации патча с другими включёнными сабмодами не найдено.

При установке CK3 и лаунчер были закрыты. Запуск игры для визуальной проверки не выполнялся.

Для просмотра подготовленного порядка без изменений:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/Install-AGOTLocalizations.ps1
```

Параметр `-Apply` выполняет регистрацию с резервным копированием. Скрипт проверяет, что активен набор `AGOT-RUSBAR`, и требует закрытых игры и лаунчера. Доступ к базе реализован через системную библиотеку Windows `winsqlite3.dll`; устанавливать Python или SQLite отдельно не требуется.
