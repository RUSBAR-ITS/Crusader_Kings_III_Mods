# Переподключение локальных модов после переезда репозитория

Все 18 зарегистрированных локальных модов CK3 из этого репозитория подключены к `D:\Repos\Crusader_Kings_III_Mods`.

## Изменения

- Для 11 модов AGOT обновлены `path` в дескрипторах репозитория и их установленных копиях в профиле CK3, а также `dirPath` в `launcher-v2.sqlite`.
- Для ACR_RE, DICM_RBE, HM_RE, RB_MTS60, RB_UD, TCC и TCC_HM_RE перенаправлены существующие junction-ссылки из каталога `mod` профиля на одноимённые папки нового репозитория. Дескрипторы этих семи модов продолжают использовать прежние адреса внутри профиля, которые теперь разрешаются в новые папки.
- В пяти актуальных манифестах локализаций пересчитан только `ExternalDescriptorSHA256`: изменился путь в соответствующем внешнем дескрипторе. Контрольные суммы содержимого модов и исходников Workshop сохранены.

Идентификаторы модов, включённость и позиции во всех наборах сохранены. Записи Workshop не изменены. Старый репозиторий не изменялся.

## Проверки

- Все 18 дескрипторов, путей лаунчера и ссылок разрешаются в ожидаемые папки нового репозитория; внутренние `descriptor.mod` доступны.
- Таблицы двух наборов, `RUSBAR` и `AGOT-RUSBAR`, совпадают с резервной копией. `dlc_load.json` с 32 включёнными модами сохранён побайтово.
- `PRAGMA quick_check` возвращает `ok`; нарушений внешних ключей нет.
- Все 84 выходных файла AGOT+ FIX доступны и совпадают с манифестом по SHA-256. Максимальная длина абсолютного пути к этим файлам теперь 208 символов.
- Восемь проверок локализаций прошли: AGOT, AGOT+, Bookmarked, Divine Intervention, COW AGOT, Crowns of Westeros, Legacy of the Dragon, Valyrian Steel.
- Проверка More Dragon Eggs остановилась на изменившемся исходнике Workshop `3736931686/localization/russian/dp_domicile_buildings_l_russian.yml`. Ожидаемая и фактическая суммы записаны в `validation.json`. Этот внешний файл не изменялся при переподключении; базовая контрольная сумма сохранена для отдельного анализа изменений.
- LF сохранён; `git diff --check` проходит.

Игра не запускалась. Полный набор аналитических проверок AGOT+ не запускался: здесь проверялись подключения, неизменность выходных файлов и проверки локализаций. Исторические отчёты и сохранённые абсолютные пути старых анализов не переписывались; их переносимость — отдельная задача.

## Резервная копия

`C:\Users\RUSBAR\Documents\Paradox Interactive\Crusader Kings III\mod_backups\repository_relocation_20260919_144807_748907`

Копия содержит базу лаунчера до изменения, `dlc_load.json`, исходные 27 изменённых файлов, их соответствие именам в `changes.json` и прежние цели junction-ссылок в `plan.json`. Перед записью проверено, что CK3 и лаунчер закрыты. Обновление базы выполнено транзакционно.

Подробные результаты и список всех подключений: [validation.json](validation.json).
