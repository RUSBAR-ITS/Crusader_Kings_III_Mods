--- before/common/buildings/yy_agotcities_special_buildings_westeros.txt
+++ after/common/buildings/yy_agotcities_special_buildings_westeros.txt
@@ -1822,11 +1822,12 @@
         culture_likely_to_fortify_modifier = yes
     }
 
-    on_complete = {
-        barony.holder = {
-            trigger_event = agot_cities.5000
-        }
-    }
+    # USF: agot_cities.5000 has no definition in the installed mod set; original call retained for reference
+# on_complete = {
+#         barony.holder = {
+#             trigger_event = agot_cities.5000
+#         }
+#     }
 
     type = special
 
@@ -1988,11 +1989,12 @@
         culture_likely_to_fortify_modifier = yes
     }
 
-    on_complete = {
-        barony.holder = {
-            trigger_event = agot_cities.5000
-        }
-    }
+    # USF: agot_cities.5000 has no definition in the installed mod set; original call retained for reference
+# on_complete = {
+#         barony.holder = {
+#             trigger_event = agot_cities.5000
+#         }
+#     }
 
     type = special
 
@@ -2290,11 +2292,12 @@
         culture_likely_to_fortify_modifier = yes
     }
 
-    on_complete = {
-        barony.holder = {
-            trigger_event = agot_cities.5000
-        }
-    }
+    # USF: agot_cities.5000 has no definition in the installed mod set; original call retained for reference
+# on_complete = {
+#         barony.holder = {
+#             trigger_event = agot_cities.5000
+#         }
+#     }
 
     type = special
 
