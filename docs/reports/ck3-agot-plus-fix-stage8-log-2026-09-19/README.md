# AGOT+ FIX: проверка запуска ревизии 8

**Запуск 19.09.2026 в 06:58 подтвердил исчезновение всех 217 целевых и связанных сообщений последних двух пакетов. Новых сообщений не обнаружено.** Проверен запуск и выход из игры; реальные рождения, автосвязь, внешний вид и клики в интерфейсе этим тестом не подтверждаются.

Сравнение с [последним проверенным запуском ревизии 6, 03:45](../ck3-agot-plus-fix-stage6-log-2026-09-19/README.md). Отдельного промежуточного игрового запуска ревизии 7 не было. Модификации игрового кода при этой проверке не выполнялись.

## Что исчезло

| Группа | Было | Осталось |
| --- | ---: | ---: |
| История персонажей: DNA, неполный комментарий, отсутствующая династия и устаревшая черта | 103 | 0 |
| Связанная вторичная ошибка `PostValidate` | 1 | 0 |
| Ссылки на отсутствующих персонажей — доноров внешности | 80 | 0 |
| Скрипты и `on_action`: восемь разобранных причин | 25 | 0 |
| Переменные рождения четырёх дочерей Мегетт используются, но не устанавливаются | 8 | 0 |
| **Всего** | **217** | **0** |

В последних восьми сообщениях каждый из `Targaryen_98`–`101` фигурировал дважды. После реализации рождений и инициализации соответствующие переменные теперь устанавливаются. Эти сообщения не входили в прежний список 25 ошибок, поскольку не содержали пути источника; они выделены отдельно.

По всем целям первого–шестого пакетов регрессий нет. Все 386 ранее исправленных сообщений о локализации также отсутствуют. [Проверка каждой цели](fix-verification.csv), [машинный результат](startup-validation.json), [сравнение](comparison-summary.json).

## Полный лог и границы атрибуции

| Показатель | Ревизия 6 | Ревизия 8 |
| --- | ---: | ---: |
| Все записи `error.log` | 10 777 | 10 560 |
| Проверенные дубли локализации | 1 190 | 1 190 |
| Класс графических/портретных ошибок | 8 890 | 8 890 |
| После исключения двух предыдущих классов | 697 | 480 |
| Сообщения, прямо отнесённые к AGOT+ или файлу его патча, кроме графического класса | 141 | 12 |
| Из предыдущей строки, вне путей `gfx/portraits` | 129 | 0 |

Сравнение выполнено по полному набору записей, включая дубли локализации. Убраны только время заголовка, технические номера `args` и сдвинувшиеся номера строк исходников. Компонент, путь, ID, текст и число повторений сохранены. Получилось **0 добавленных и 217 исчезнувших записей**, без необходимости исключать вариации известного дубля события короны. [Добавления](new-messages.csv), [исчезнувшие записи](disappeared-messages.csv).

Нулевой остаток в последней строке таблицы относится к строгой атрибуции по путям, а не ко всем возможным проблемам AGOT+. В логе остаются сообщения без пути источника, сообщения других модов и графические ошибки.

Полный остаток, включая графику и отдельную атрибуцию сообщений без пути, разобран в [дополнительном перечне AGOT+](../agot-plus-remaining-inventory-2026-09-19/README.md): 8 840 связанных сообщений, с повторениями и предупреждениями. Этот расширенный срез не меняет результат сравнения запусков и не означает появления новых ошибок.

## Оставшиеся сообщения

Все 12 записей, прямо связанных с AGOT+ вне общего графического класса, находятся в портретных скриптах:

| Файл | Проблема | Сообщений |
| --- | --- | ---: |
| `asoiaf_character_custom_flags.txt` | Нет `portrait_wear_lannister_armour_torso_courtiers` | 1 |
| `asoiaf_character_custom_flags.txt` | Неизвестное свойство `accesory` | 1 |
| `asoiaf_clothes.txt` | Некорректное `liege.has_religion`, две позиции | 4 |
| `asoiaf_headgear_crowns.txt` | Отсутствующие `is_character_baratheon_3`–`7` | 6 |

Одна запись может перечислять несколько неизвестных условий. Поэтому 12 — количество сообщений, не число отдельных неисправностей. Они присутствовали и раньше; исправления этой проверки их не затрагивают. [Полный список](agot-plus-remaining.csv).

Также сохранились **15 сообщений по шести другим ссылкам на персонажей**: `Gardener_75` (2), `Dormund_1` (4), `Broome_rs_54` (1), `Hotah_rs_2` (2), `asoiaf_Targaryen_61_1` (2), `Targaryen_61_1` (4). Это прежний остаток вне восстановленных доноров внешности. Их исходные места уже перечислены в [анализе истории](../agot-plus-character-history-analysis-2026-09-19/character-link-reference-sites.csv); факт отсутствия персонажа сам по себе не обосновывает замену другой личностью. [Записи нового лога](remaining-character-links.csv).

## Как подтверждён проверяемый состав

- Игра начала журнал в **06:58:16**, последняя запись ошибки — **07:00:50**; в `debug.log` выход инициирован из игры в **07:01:52**. Временная зона `+07:00`.
- Набор и порядок **32 модов** совпадают с предыдущим запуском, включая содержимое `dlc_load.json`; патч по-прежнему четвёртый.
- Журнал монтирования подтверждает загрузку рабочей папки `AGOT_PLUS_FIX`. [Доказательства из debug.log](debug-mount-evidence.json).
- Проверены **45 игровых файлов патча и восемь файлов коррекции локализации**: хеши совпадают с манифестами; время записи предшествует запуску. В активном наборе эти пути не перекрываются последующими модами.
- Проверены **100 хешей исходных файлов** AGOT+/AGOT; источники не изменялись после подготовки исправления. [Таблица проверки](source-hash-verification.csv).
- Сохранённые результаты статической проверки седьмого и восьмого пакетов привязаны к тому же манифесту ревизии 8.

SHA256 проверенного манифеста: `22921E90C8B0FF497FFD77482F5D14B9F4E2FB287907D7E23838BB71FDCD505C`.

SHA256 нового `error.log`: `66781C0707B4EF82A775FBA0C6B3A72360135D2B9982E1D0FB7136A7D8FEBB03`.

Оригиналы логов и манифестов сохранены в `snapshot/`; контрольные суммы основных логов — в [snapshot-summary.json](snapshot-summary.json). Профиль, Workshop, сохранения и игровые файлы при аудите не изменялись.

Повторная проверка сохранённого снимка из корня репозитория:

```powershell
python docs/reports/ck3-agot-plus-fix-stage8-log-2026-09-19/Verify-Run.py
```

Это подтверждение устранения диагностик загрузки. Работа новых рождений, сохранение отношений и домов, портреты в партии, выбор ветви дома, наследник, регентство и осады требуют отдельных игровых наблюдений.
