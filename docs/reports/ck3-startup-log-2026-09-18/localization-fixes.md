# Исправления локализации после аудита

Выполнено 18.09.2026 по следующему запросу пользователя: исправить русификатор AGOT, а правки базового Divine Intervention и AGOT-дополнения включить в наш `Divine_Intervention_AGOT_RUS`. Дубли, портреты ванильных сохранений и механические ошибки субмодов в эту работу не входят.

## AGOT_RUS_CORRECT

- Восстановлен пробел после `:0` у `lmf_address_cousin` и `sc_harpy_adherent_plural`.
- Удалены четыре неправильных экранирования `\«` / `\»` в трёх строках событий.
- У `admin_vassal_tab_appointment` исправлены скобки/фильтр и опечатка в обработчике `GetGovernorConceptPossessiveNoTooltip`. Обработчик существует в текущем AGOT и CK3.
- В `game_concept_exam_score_desc` восстановлены отдельные корректные ссылки на учёность и управление.
- Все семь значений включены в файл замен; исходные файлы подменены по тем же путям. Итого 157 ключей в файле замен, 20 подменённых файлов, 21 файл локализации.

Сборка и проверка прошли: сохранены девять прежних исправлений кавычек; проверены семь новых правок, синтаксис, допустимые экранирования, BOM, ссылки и хеши исходников. Манифест позволяет восстановить каждый файл из исходника и точного списка изменений.

## Divine_Intervention_AGOT_RUS

- Подсказка стресса базового DI вызывает `AddTextIf`; видимый заголовок `Effects` переведён.
- Две точки за закрывающими кавычками удалены из подсказок выбора королевств и графств; исправлено «все графства».
- Добавлены 12 подписей построек, 13 названий реально существующих войск AGOT и подпись отладочной кнопки создания шкуры.
- Теперь в файле замен 102 ключа. Дополнительно включена копия одного исходного файла внешнего DI RU: ровно три изменения, остальные 1051 ключ сохранён. Отдельный русификатор базового DI по-прежнему нужен для остальных файлов.
- Проверка расширена с `DI_*` на подписи построек и имена создаваемых армий. Проверены 774 уникальных ключа, неразрешённых ссылок нет; проверен 81 хеш исходников.

Определения и поведение игровых объектов не менялись, добавлена только локализация. В частности, графический объект `special_kings_landing_not_explosion_graphical` назван по фактическому эффекту `env_wildfire01`, а не по вводящему в заблуждение фрагменту его внутреннего имени. Ключ с исходной опечаткой `wyven_hunters` сохранён, чтобы меню продолжало обращаться к существующему типу войск.

## Применение

Прочитаны зарегистрированные `.mod`: оба уже указывают на соответствующие папки репозитория и включены в `dlc_load.json`. Новых модов и изменений набора не требуется. Для загрузки правок нужен полный перезапуск CK3.

Workshop-источники, файлы GUI/механик и прежний лог не изменены. Отсутствие этих ошибок в новом игровом логе пока не проверено: статические проверки не подменяют следующий запуск движка.
