# Проверка исправления Тёмной Сестры

Проверен запуск **22 сентября 2026 года, 15:50:49–15:52:49**. Сравнение выполнено с [предыдущим запуском 14:41–14:42](../ck3-without-crowns-log-2026-09-22/README.md). Состав и порядок 27 модов, пути и `replace_path` в их дескрипторах совпадают.

**Исправление подтверждено при загрузке: исчезли все восемь сообщений об отсутствующем `vs_dark_sister_template`. Новых сообщений нет.** После удаления временных меток и завершающих переводов строк все остальные записи `error.log` совпадают с предыдущим запуском по содержимому и числу повторов. Сохранены [точные различия](differences.json), [сводка](summary.json), [классификация](diagnostics.csv) и [снимки журналов](snapshot).

## Результат последних изменений

| Проверка | До | После |
|---|---:|---:|
| Ошибки шаблона Тёмной Сестры | 8 | **0** |
| Все сообщения об отсутствующих шаблонах артефактов | 8 | **0** |
| Все записи `error.log` | 2782 | 2774 |
| Дубли локализации, исключённые из анализа | 1135 | 1135 |
| Остальные сообщения `error.log` | 1647 | **1639** |
| Записи `gui_warnings.log` | 483 | 483 |

Эффективный поставщик `common/scripted_effects/00_agot_artifact_vs_sword_effects_override.txt` — **AGOT Submods | Единые исправления**. AGOT+ и отдельный AGOT_PLUS_FIX не участвуют в загрузке, поэтому исчезновение ошибки не объясняется возвратом их переопределения.

**Исключение Crowns сохранено:** восемь архивных файлов отсутствуют в игровых каталогах общего фикса; исходный мод и его переводы выключены. События заказа корон из `events/activities/agot_coronation_activity/agot_coronation_crown_commission_events.txt` по-прежнему поставляет основной AGOT. Упоминания Crowns в интеграциях Submod Core сохраняются, как и в предыдущем запуске.

## Оставшиеся категории

| Категория | Сообщений | Состояние |
|---|---:|---|
| Недоступные аксессуары в группах генов | 640 | Прежние интеграции Submod Core с отсутствующими сабмодами |
| Скрипты, параметры и условия | 318 | 283 — Submod Core, 35 — обычный DI; восемь сообщений Тёмной Сестры устранены |
| Переменные/флаги читаются, установка не найдена | 558 | Без изменений: 290 Core, 266 значений зданий HM, 2 FAB |
| Переменные/флаги устанавливаются, чтение не найдено | 74 | Прежние маркеры; `dark_sister_artifact` не относится к исправленному шаблону и остаётся |
| UTF-8 без BOM | 36 | Прежние 32 файла нашего DI-дополнения; кодировки в этом этапе не менялись |
| Некорректное описание аксессуара | 2 | Прежнее `shared_pose_entity = hea` у `maesteraemon_crown` в Core |
| Дубли текстур | 2 | Согласованно сохранённые Ethiopian atlas |
| Ссылка на отсутствующего персонажа | 2 | Прежние ссылки FAB на `character:fab_aurion` |
| Сохранённые наборы правил | 3 | Прежние составные сообщения о пресетах |
| Прочие предупреждения | 4 | Clear Notifications, событие маски Legacy без найденного вызова, достижения |
| **Итого без дублей локализации** | **1639** | Новых сообщений нет |

Числа учитывают повторные проходы проверки и не равны количеству отдельных поломок. Атрибуция перенесена из предыдущего отчёта только для сообщений, точно совпавших по уровню, источнику и тексту. `game.log` не прибавляется повторно к общей сумме. Все 483 GUI-предупреждения совпадают с предыдущими по содержимому и кратности.

## Границы проверки

По [debug-evidence.txt](debug-evidence.txt) игра завершила загрузку главного меню и вышла из него. Загрузка кампании не зафиксирована; получение или создание меча и нажатия интерфейса этим запуском не проверены. Для данной ошибки подтверждено именно прохождение проверки шаблона при старте.

Игровые файлы, профиль, порядок загрузки и Workshop в этом аудите не изменялись; игра агентом не запускалась. Журналы не изменились во время снятия и проверки снимка. По решению пользователя установка Legacy of Valyria отменена; в проверенном наборе его нет.
