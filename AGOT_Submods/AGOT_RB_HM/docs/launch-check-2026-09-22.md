# Проверка запуска 22 сентября 2026 года

Проверен `Documents/Paradox Interactive/Crusader Kings III/logs/error.log`, последнее изменение — **22.09.2026 05:53:11**, размер 528 751 байт. В `dlc_load.json` мод `AGOT_RB_HM.mod` включён последним, после Валирийского дополнения. Регистрация и порядок загрузки соответствуют установке.

## Диагностика Holding Manager

| Причина | Сообщений | Исправление |
|---|---:|---|
| `ignoreinvisible` задан кнопке, которая его не поддерживает | 1172: по два для каждой из 586 строк | Параметр перенесён в родительский `vbox`; скрытые строки не занимают место |
| Несуществующая функция `GetPlayer.HasVariable` | 4, включая последующие ошибки разбора `visible` | Видимость меню проверяет `rbhm_menu_context` через scripted GUI |
| Коллизия хеша `rbhm_choice_agot_grassfield_florestries_01` с ванильным `feature_decoration_pattern_quotes` | 2: коллизия и отсутствующий текст | Изменён только ключ подписи этого здания, согласованно в GUI и обеих локализациях |
| UTF-8 без BOM в пяти игровых `.txt` | 6: файл script values прочитан дважды | UTF-8 с BOM, переносы LF; генератор сохраняет этот формат |
| **Всего** | **1184** | **Четыре причины** |

Исходная коллизия имела хеш `-1161335086`. Новый ключ — `rbhm_pick_label_agot_grassfield_florestries_01`; ID самого здания, цена и требования сохранены.

Проверка меню также привязана к выбранной провинции: сохраняются наличие обеих переменных и совпадение провинции заказа с текущей. Строки проверяют категорию меню; нажатие повторно проверяет эти условия перед оплатой. Смена просматриваемого владения не разрешает выполнить прежний заказ в другом месте.

## Проверка и сохранения

Генератор исправлен вместе с игровыми файлами. Пройдены **26 автоматических проверок**, включая проверки разметки списка, ссылок подписи, формата скриптов и сохранённые проверки требований, цен и очередности применения.

Новая кампания для этих исправлений не требуется: меняются интерфейс, проверка меню и формат файлов. Порядок выбора взаимоисключающих зданий инициализируется при открытии владения, а не только при начале кампании. Нужен полный перезапуск игры для загрузки новых GUI/локализации, затем можно открыть прежнее сохранение.

После исправлений CK3 ещё не запускалась. Приведённые количества относятся к исходному логу, а не к подтверждённому исчезновению сообщений. Отображение, нажатия, списание ресурсов и отсутствие новых ошибок нужно проверить следующим запуском.

## Другие сообщения

В том же логе есть отдельные ошибки Валирийского дополнения: поиск культуры/веры в `agot_di_rusbar_eye.gui` передаёт один аргумент вместо двух в `GetVariableSystem.Set`, знаки «+»/«−» заданы как ключи локализации; также есть предупреждения о BOM в его скриптах. Они не принадлежат Holding Manager и в этой правке не изменялись.

Есть сообщения DI, портретных модов и других компонентов сборки. Сообщение `Failed to read key reference` с пустым именем файла не позволяет установить источник по одному логу; оно не включено в число ошибок HM. Результат этой проверки не означает, что весь лог сборки очищен.

## Повторная проверка: кнопки не появились

В следующем проверенном логе (запуск 06:05–06:15, 387 974 байта) перечисленные 1184 сообщения HM больше не обнаружены. Пользователь подтвердил: прежняя кнопка «+» видна, шесть новых кнопок отсутствуют. Загрузка GUI-файлов подтверждается `debug.log`; мод по-прежнему включён последним.

Причина найдена в генераторе: `window_county_view.gui` содержит **три** окна — `holding_view`, `holding_tracks_view`, `holding_type_selection_view`. Вставка перед разделом `TYPES` помещала панель и меню в последнее из них, то есть в окно выбора типа владения. Это синтаксически допустимо и поэтому не порождало ошибок разбора, но на основном экране зданий элементы отсутствовали.

Исправление: генератор находит именно окно `holding_view` по имени и его закрывающую скобку с учётом вложенности, строк и комментариев. Панель и меню вставлены внутрь этого окна; два других окна сохранены без изменений. У панели явно задано совмещение нижнего левого угла с родителем, чтобы её высота размещалась над нижним отступом.

Пройдены **28 проверок**. Новые проверки контролируют принадлежность элементов нужному окну, неизменность соседних окон и обработку скобок внутри строк/комментариев. Предыдущие проверки наличия элементов где-либо в файле эту ошибку не выявляли. Отображение после переноса ещё требует проверки в игре; существующее сохранение подходит.

## Следующий запуск: панель по-прежнему отсутствует

Пользователь прислал новый снимок с видимой кнопкой «+» и отсутствующей панелью HM. В логе запуска 06:22–06:24 (`error.log`, 384 214 байт, последнее изменение 06:24:56) нет сообщений `rbhm` или ошибок `window_county_view`. `debug.log` подтверждает загрузку `rbhm_controls.gui` в 06:22:39 и `window_county_view.gui` в 06:22:41; HM подключён после Валирийского дополнения. Следовательно, отсутствие ошибок разбора и попадание элементов в правильное окно не доказывают их видимость.

Внесены дополнительные изменения в `tools/build_runtime.py` и соответствующие игровые файлы:

- `rbhm_controls` помещён в штатный левый `vbox` нижнего блока зданий, перед его растяжкой. Привязка панели к номинальному размеру внешнего окна удалена; панель наследует контекст содержимого экрана.
- Видимость панели использует тот же вызов `rbhm_legacy_add_slot.IsShown` с игроком и провинцией, что и видимая кнопка «+». Убрана дополнительная общая проверка выбора здания.
- Три основные кнопки наследуют видимость панели. Их `IsValid`, подсказки и действия сохранены. Три специальные кнопки сохраняют собственные условия появления.

Это устраняет зависимость общей видимости от расширенного набора строительных областей действия и меняет размещение панели. Какая из этих зависимостей препятствовала отображению в предыдущем варианте, отдельно не доказано. Условия строительства, цены и эффекты не изменены.

Был выполнен отдельный тестовый запуск с копией профиля во временной папке. Загрузить нужное сохранение и получить подтверждение отображения кнопок в нём не удалось. Этот запуск не считается успешной проверкой интерфейса. Пользователь запретил самостоятельные запуски игры; тестовый процесс завершён, дальнейшие проверки агента ограничены файлами и логами. Рабочий профиль, лаунчер и сохранения пользователя в этой проверке не изменялись.

Появление панели и выполнение действий после последней правки **ещё не подтверждены**. Следующий игровой запуск выполняет пользователь.

## Лог 06:41: сравнение с рабочим «+»

После нового сообщения пользователя проверен свежий `error.log` размером 1 285 039 байт. Найдены конкретные ошибки последнего варианта панели:

| Сообщение | Количество | Исправление |
|---|---:|---|
| `Property 'margin_left' not handled` и `Property 'margin_bottom' not handled`, `gui/rbhm_controls.gui` | 2 | Поля удалены из `widget`; свободное место задано его размером и координатами дочерних кнопок |
| `Error setting properties for 'rbhm_controls' (widget)`, `window_county_view.gui:1765` | 1 | Исправлены вызвавшие ошибку свойства панели |
| Чтение отсутствующих `rbhm_order_0`, `_1`, `_2` | 720 | Все обращения внутри выбора конфликтных групп защищены отдельной веткой `trigger_if` с `has_variable`; при отсутствии переменной результат — `always = no` |
| Последующие ошибки `var`: отсутствующая область действия и некорректное сравнение | 1440 | Устранено предшествующее чтение неустановленной переменной |

Рабочий «+» объявлен как `button_icon` в `rbhm_legacy_slot.gui` и вставлен в штатный `flowcontainer` рядом с особыми ячейками. Каждая новая кнопка также имеет тип `button_icon`, но они вложены в общий `widget`. Неподдерживаемые поля находились именно у этой общей панели; у «+» их нет. Ошибка создания панели зафиксирована движком, а не выведена из одного только отсутствия кнопок на снимке.

Размер панели теперь 116×184; кнопки остаются 52×52 с шагом 58, левый отступ задаётся координатой X=4. Место вставки сохраняется в левой колонке блока зданий. Условие общей видимости совпадает с рабочим «+»; проверки нажатия, оплаты и исходных требований зданий сохранены.

Инициализация случайного порядка перенесена с `on_finish` на `on_start` состояния. Независимо от порядка вычисления GUI условия не читают отсутствующие переменные; новый порядок не подменяется фиксированным значением и не пересчитывается при каждом наведении.

Регрессионные проверки сначала воспроизвели ошибки на прежних файлах: неподдерживаемые свойства панели и обращение к отсутствующей переменной. После исправления генератора и выпуска файлов проходят **29 проверок**. Прежний тестовый интерпретатор незаметно подставлял `−1` вместо отсутствующей переменной; теперь такое чтение вызывает ошибку и проверяется отдельно для полностью и частично неинициализированной провинции. Игровой запуск агентом не выполнялся. Визуальная проверка панели и проверка нажатий остаются за пользователем.

## Лог 06:54 и текст поверх экрана

Следующий `error.log` (последнее изменение 06:54:53, 383 838 байт) больше не содержит ошибок `rbhm` и `window_county_view`. На снимке пользователя виден текст подсказки улучшения обычных зданий без фона и рамки; на нём также нет различимых кнопок. Исправление ошибок разбора не завершило работу над отображением.

В генераторе у подсказки был только `GeneralTooltipSetup`. В установленном `game/gui/preload/tooltip.gui` этот шаблон задаёт обработку мыши и закрытия; фон и рамку создаёт отдельный `DefaultTooltipBackground`. Добавлен такой же дочерний фоновый `widget`, как в штатном `DefaultTooltipWidget`, с размером 100%×100% и непрозрачной для мыши областью подсказки. Текст использует `DefaultTooltipText` с ограничением ширины 440 внутри прокрутки 480×400. Внешние отступы заданы поддерживающему их `flowcontainer`. Список сохраняется целиком; жёстко вписанные маркеры «•» убраны, поскольку `custom_tooltip` уже добавляет их в отображении эффекта.

Отдельно проверено наследование `button_icon` из `game/gui/shared/buttons_icons.gui:821`: этот тип задаёт состояния 1–4 и накладывает `Master_Button_Modify_Texture` с цветовым атласом. Важно: **сама текстура «+» однокадровая, 60×60**, многокадровым является её цветовой атлас 960×96. Поэтому утверждать по одному наследованию кадров, что именно оно скрыло наши кнопки, нельзя. Для готовых цветных изображений HM всё же убрана зависимость от этого оформления: пустой override цветового слоя, явный размер кадра 128×128 и кадр 1 во всех семи состояниях. Выбор отдельного активного/пассивного DDS по доступности действия сохранён.

Проходят **31 проверка**. Дополнительные проверки сопоставляют номера кадров и размер кадра с реальными DDS, контролируют наличие фона, ограничение ширины текста, сохранение прокрутки и размещение текста именно внутри `tooltipwidget`. Они не проверяют рендеринг CK3. Игра агентом не запускалась; внешний вид после этих изменений ещё не подтверждён.

## Лог 07:08: нажатия работают, рисунки отсутствуют

Пользователь подтвердил работу нажатий на невидимые кнопки; на втором снимке видны результаты улучшения зданий. Подсказка на первом снимке уже имеет фон, рамку и перенос строк. Предыдущая правка оформления подсказки сработала, а явные кадры не решили отсутствие рисунков.

В `error.log` от 07:08:24 (383 838 байт) нет сообщений HM или ошибок загрузки его DDS; `debug.log` подтверждает загрузку GUI. Файлы DDS читаются как RGBA 128×128, альфа полностью непрозрачна. Маски каналов и формат несжатого DDS совпадают с форматом штатного `plus.dds`; различаются размеры и наличие mip-уровней. Это не равно проверке отображения текстур движком, но не даёт основания перепаковывать изображения наугад.

У всех шести кнопок оставалось выражение `texture = "[Select_CString(...)]"`, возвращающее строку пути. У рабочего «+» ресурс задан напрямую; в просмотренных штатных GUI CK3 и AGOT аналогичного выбора `texture` через `Select_CString` не найдено. Убрана эта непроверенная связка: пассивный DDS теперь прямо указан в `texture` кнопки, а активный — в `texture` дочернего `icon`. Активный слой виден при том же `IsValid`, что разрешает нажатие, и не перехватывает мышь. Панель, расположение, подсказки, цены и строительные действия не менялись.

Проходят **32 проверки**: проверяются прямые ссылки на все 12 существующих DDS, допустимые кадры, согласованность состояния рисунка с доступностью кнопки и сохранение обработки клика родителем. Связь исходной невидимости именно с возвратом строки остаётся выводом из сравнения, а не подтверждённой диагностикой движка. Отрисовку после изменения ещё должен проверить пользователь; игра агентом не запускалась.

## Лог 07:52: создание герцогской постройки работает, улучшение отклоняется

Пользователь сообщил, что доступное улучшение герцогской постройки ничего не делает, тогда как первое строительство работает. `error.log` с последним изменением 07:52:17 (682 331 байт) содержит **42** отказа `add_building` из `rbhm_duchy:effect`: провинция «Орлиное Гнездо» (2251) уже имеет герцогскую постройку. Строка 15834 сгенерированного файла на момент запуска — `add_building = military_academy_02`. То есть действие пыталось добавить Военную академию II в занятую ячейку вместо замены Военной академии I.

В том же логе есть **6** отказов `set_great_building` из `rbhm_great:effect`: в провинции уже существует великая постройка. Затронуты Блошиный Конец, Поля Гавани и Королевская Гавань. Это отдельные сообщения того же класса; ошибки локализации DI к ним не относятся.

Исправлен общий генератор одиночных действий:

- **51 герцогское, 130 особых и 6 великих улучшений** теперь используют `replace_building_effect = <целевой_уровень>`. Проверка точного предыдущего уровня сохранена; здание предварительно не удаляется. Начальное строительство остаётся через `add_building`/`add_great_building`.
- Форма эффекта подтверждена встроенной справкой установленного `ck3.exe`, прочитанной без его запуска: эффект заменяет здание на указанный ключ и поддерживает герцогскую, особую и великую ячейки. Примеры есть в базовом `common/great_projects/types/00_great_project_types.txt:3562` и AGOT `common/great_projects/types/00_agot_chroyane_great_projects.txt:389`.
- Раньше оплата происходила до эффекта, поэтому отклонённая попытка могла списывать ресурсы. Теперь цена фиксируется перед изменением владения, а списание выполняется только при наличии целевого здания после эффекта. При отказе меню выбора остаётся открытым. Географические улучшения взносов великих проектов также защищены проверкой целевого здания.
- Исправлены оба пути: единственный доступный вариант и выбор из списка. Главная постройка и массовые действия с обычными зданиями в этой правке не менялись.

Проходят **35 проверок**. Новые проверки контролируют нативный эффект и предыдущий уровень для всех уникальных вариантов; небольшая модель исполняет сгенерированные тела действий для создания, успешного улучшения и отказа. Она проверяет все четыре валюты, неизменность цены после изменения владения и закрытие меню только при успехе. Это регрессионная проверка последовательности скрипта, а не исполнение движка CK3. Игра агентом не запускалась; улучшение в текущем сохранении нужно подтвердить следующим запуском пользователя. Ранее потраченные ресурсы в сохранениях не изменялись.
