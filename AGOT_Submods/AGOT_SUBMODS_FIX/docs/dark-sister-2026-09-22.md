# Тёмная Сестра: действующий шаблон артефакта

В запуске 22 сентября 14:41–14:42 две ссылки на отсутствующий `vs_dark_sister_template` дали восемь сообщений проверки данных. После отключения AGOT+ и его отдельного исправления эти ссылки оставались в функции Legacy of the Dragon, которую перекрывает общий фикс.

В `common/scripted_effects/00_agot_artifact_vs_sword_effects_override.txt`, внутри `agot_create_artifact_vs_dark_sister_effect`, обе ссылки заменены на действующий `valyrian_steel_template` текущего AGOT. Изменены только две строки. Сохранены обе модели (`darksister` и `vs_dark_sister_visuals`), их выбор игровым правилом, `vs_dark_sister_modifier`, имя, история, владелец, экипировка и маркеры. Переменная `dark_sister_artifact` этим исправлением не затрагивается.

Сборщик `Build.py` и выходные контрольные суммы обновлены. Общие проверки дополнительно проверяют оба варианта создания и их модели. Ресурсная проверка восстанавливает прежние две ссылки в памяти и сверяет SHA-256 всего файла с исторической контрольной суммой: посторонние изменения в нём не разрешены.

Проверки после сборки: **229 общих**, **362 ресурсных**; все прошли. Число ресурсных проверок включает сверку с текущим набором загрузки и не обязано совпадать с числом для прежнего набора. Остальные 78 игровых файлов не изменены. Workshop и профиль загрузки не редактировались; игра агентом не запускалась. Это исправление функции создания, оно не переписывает уже существующие артефакты в сохранении.

**Подтверждение запуском пользователя:** [лог 22 сентября 15:50:49–15:52:49](../../../docs/reports/ck3-dark-sister-log-2026-09-22/README.md) больше не содержит ни одного из восьми сообщений. Новых диагностик нет; все остальные сообщения совпадают с предыдущим запуском по содержимому и кратности. Проверена загрузка главного меню; создание меча в кампании этим запуском не проверялось.
