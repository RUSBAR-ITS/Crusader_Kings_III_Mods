NProvince = {
	# HM_RE uses a practical limit of 200 slots. Raise the engine ceiling too;
	# otherwise additional modifiers stop producing usable slots at 7 (AGOT)
	# or 10 (COW-AGOT). This ceiling does not grant any slots by itself.
	MAX_BUILDINGS = 200
}
