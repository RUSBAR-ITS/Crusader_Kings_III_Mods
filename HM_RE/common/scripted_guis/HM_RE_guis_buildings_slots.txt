HM_RE_build_all_button = {
    scope = character
	saved_scopes = { gui_holding }
    is_shown = {
		NOT = { has_game_rule = HM_RE_rule_player_extra_slots_disabled }
		has_game_rule = HM_RE_build_all_button_on
		scope:gui_holding = {
			NOT = { has_holding_type = nomad_holding }
			barony = { is_under_holy_order_lease = no }
			OR = {
				HM_RE_trigger_player_add_slot = yes
				has_free_building_slot = yes
			}
		}
		trigger_if = {
			limit = {
				always = yes
			}
			trigger_if = {
				limit = {
					scope:gui_holding.barony = { is_leased_out = no }
				}
				OR = {
					scope:gui_holding.province_owner = root
					is_liege_or_above_of = scope:gui_holding.province_owner
				}
				
			}
			trigger_else = {
				OR = {
					scope:gui_holding.barony.lessee = root
					is_liege_or_above_of = scope:gui_holding.barony.lessee
				}
			}
		}
    }
	is_valid = {
		scope:gui_holding = {
			HM_RE_can_construct_buildings = yes
			has_ongoing_construction = no
			trigger_if = {
				limit = {
					always = yes
					root = {
						has_treasury = yes
					}
					has_game_rule = HM_RE_player_cost_treasury
				}
				HM_RE_value_max_building_slots_and_construct_buildings_gold_cost <= root.treasury
				HM_RE_value_construct_buildings_prestige_cost <= root.prestige
			}
			trigger_else = {
				limit = {
					always = yes
				}
				HM_RE_value_max_building_slots_and_construct_buildings_gold_cost <= root.gold
				HM_RE_value_construct_buildings_prestige_cost <= root.prestige
			}
		}
    }
    effect = {
		scope:gui_holding = {
			if = {
				limit = {
					always = yes
					root = {
						has_treasury = yes
					}
					has_game_rule = HM_RE_player_cost_treasury
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_max_building_slots_and_buildings_construction_treasury
					value = HM_RE_value_max_building_slots_and_construct_buildings_gold_cost
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_buildings_construction_prestige_cost
					value = {
						value = HM_RE_value_construct_buildings_prestige_cost
						multiply = -1
					}
				}
				root = {
					remove_treasury = scope:HM_RE_gui_scope_max_building_slots_and_buildings_construction_treasury
					add_prestige = scope:HM_RE_gui_scope_buildings_construction_prestige_cost
				}
			}
			else_if = {
				limit = {
					always = yes
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_max_building_slots_and_buildings_construction_gold
					value = HM_RE_value_max_building_slots_and_construct_buildings_gold_cost
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_buildings_construction_prestige_cost
					value = {
						value = HM_RE_value_construct_buildings_prestige_cost
						multiply = -1
					}
				}
				root = {
					remove_long_term_gold = scope:HM_RE_gui_scope_max_building_slots_and_buildings_construction_gold
					add_prestige = scope:HM_RE_gui_scope_buildings_construction_prestige_cost
				}
			}
			custom_tooltip = HM_RE_build_all.tt
			hidden_effect = {
				HM_RE_effect_construct_buildings = yes
			}
		}
    }
}

HM_RE_upgrade_all_button = {
    scope = character
	saved_scopes = { gui_holding }
    is_shown = {
		scope:gui_holding = {
			NOT = { has_holding_type = nomad_holding }
			barony = { is_under_holy_order_lease = no }
		}
		has_game_rule = HM_RE_upgrade_all_button_on
		trigger_if = {
			limit = {
				always = yes
			}
			trigger_if = {
				limit = {
					scope:gui_holding.barony = { is_leased_out = no }
				}
				OR = {
					scope:gui_holding.province_owner = root
					is_liege_or_above_of = scope:gui_holding.province_owner
				}
			}
			trigger_else = {
				OR = {
					scope:gui_holding.barony.lessee = root
					is_liege_or_above_of = scope:gui_holding.barony.lessee
				}
			}
		}
    }
	is_valid = {
		scope:gui_holding = {
			HM_RE_can_upgrade_buildings = yes
			has_ongoing_construction = no
			trigger_if = {
				limit = {
					always = yes
					root = {
						has_treasury = yes
					}
					has_game_rule = HM_RE_player_cost_treasury
				}
				HM_RE_value_upgrade_buildings_gold_cost <= root.treasury
				HM_RE_value_construct_buildings_prestige_cost <= root.prestige
			}
			trigger_else = {
				limit = {
					always = yes
				}
				HM_RE_value_upgrade_buildings_gold_cost <= root.gold
				HM_RE_value_upgrade_buildings_prestige_cost <= root.prestige
			}
		}
    }
    effect = {
		scope:gui_holding = {
			if = {
				limit = {
					always = yes
					root = {
						has_treasury = yes
					}
					has_game_rule = HM_RE_player_cost_treasury
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_buildings_upgrade_treasury_cost
					value = HM_RE_value_upgrade_buildings_gold_cost
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_buildings_upgrade_prestige_cost
					value = {
						value = HM_RE_value_upgrade_buildings_prestige_cost
						multiply = -1
					}
				}
				root = {
					remove_treasury = scope:HM_RE_gui_scope_buildings_upgrade_treasury_cost
					add_prestige = scope:HM_RE_gui_scope_buildings_upgrade_prestige_cost
				}
			}
			else_if = {
				limit = {
					always = yes
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_buildings_upgrade_gold_cost
					value = HM_RE_value_upgrade_buildings_gold_cost
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_buildings_upgrade_prestige_cost
					value = {
						value = HM_RE_value_upgrade_buildings_prestige_cost
						multiply = -1
					}
				}
				root = {
					remove_long_term_gold = scope:HM_RE_gui_scope_buildings_upgrade_gold_cost
					add_prestige = scope:HM_RE_gui_scope_buildings_upgrade_prestige_cost
				}
			}			
			custom_tooltip = HM_RE_upgrade_all.tt
			hidden_effect = {
				HM_RE_effect_upgrade_buildings = yes
			}
		}
    }
}

HM_RE_remove_all_button = {
    scope = character
	saved_scopes = { gui_holding }
    is_shown = {
		debug_only = yes
		scope:gui_holding = {
			NOT = { has_holding_type = nomad_holding }
			barony = { is_under_holy_order_lease = no }
		}
    }
	is_valid = {
		scope:gui_holding = {
			HM_RE_trigger_remove_buildings = yes
			has_ongoing_construction = no
		}
    }
    effect = {
		custom_tooltip = HM_RE_remove_all.tt
		scope:gui_holding = {
			hidden_effect = {
				HM_RE_effect_remove_buildings = yes
			}
		}
    }
}

HM_RE_remove_special_buildings_button = {
    scope = character
	saved_scopes = { gui_holding }
    is_shown = {
		debug_only = yes
		scope:gui_holding = {
			NOT = { has_holding_type = nomad_holding }
			has_special_building_slot = yes
			barony = { is_under_holy_order_lease = no }
		}
    }
	is_valid = {
		scope:gui_holding = {
			has_ongoing_construction = no
			has_special_building = yes
		}
    }
    effect = {
		custom_tooltip = HM_RE_remove_special_buildings.tt
		scope:gui_holding = {
			hidden_effect = {
				HM_RE_effect_remove_special_buildings = yes
			}
		}
    }
}

HM_RE_add_slot_button = {
    scope = character
	saved_scopes = { gui_holding }
    is_shown = {
		NOT = { has_game_rule = HM_RE_rule_player_extra_slots_disabled }
		scope:gui_holding = {
			barony = { is_under_holy_order_lease = no }
			NOT = { has_holding_type = nomad_holding }
			HM_RE_trigger_player_add_slot = yes
			HM_RE_value_player_potential_slots > 0
		}
		trigger_if = {
			limit = {
				always = yes
			}
			trigger_if = {
				limit = {
					scope:gui_holding.barony = { is_leased_out = no }
				}
				OR = {
					scope:gui_holding.province_owner = root
					is_liege_or_above_of = scope:gui_holding.province_owner
				}
				
			}
			trigger_else = {
				OR = {
					scope:gui_holding.barony.lessee = root
					is_liege_or_above_of = scope:gui_holding.barony.lessee
				}
			}
		}
    }
	is_valid = {
		scope:gui_holding = {
			has_ongoing_construction = no
			trigger_if = {
				limit = {
					always = yes
					root = {
						has_treasury = yes
					}
					has_game_rule = HM_RE_player_cost_treasury
				}
				HM_RE_value_player_slot_cost <= root.treasury
			}
			trigger_else = {
				limit = {
					always = yes
				}
				HM_RE_value_player_slot_cost <= root.gold
			}
		}
    }
    effect = {
		scope:gui_holding = {
			if = {
				limit = {
					always = yes
					root = {
						has_treasury = yes
					}
					has_game_rule = HM_RE_player_cost_treasury
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_add_building_slot_cost
					value = HM_RE_value_player_slot_cost
				}
				root = {
					remove_treasury = scope:HM_RE_gui_scope_add_building_slot_cost
				}
			}
			else_if = {
				limit = {
					always = yes
				}
				save_temporary_scope_value_as = {
					name = HM_RE_gui_scope_add_building_slot_cost
					value = HM_RE_value_player_slot_cost
				}
				root = {
					remove_long_term_gold = scope:HM_RE_gui_scope_add_building_slot_cost
				}
			}
			add_province_modifier = extra_building_slot
		}
    }
}