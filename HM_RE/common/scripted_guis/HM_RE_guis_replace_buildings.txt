HM_RE_replace_button = {
	scope = character
	saved_scopes = { gui_holding }

	is_shown = {
		is_ai = no
		scope:gui_holding.province_owner = {
			OR = {
				this = root
				AND = {
					has_game_rule = HM_RE_rule_can_replace_buildings_vassals
					is_vassal_or_below_of = root
				}
			}
		}
	}
}