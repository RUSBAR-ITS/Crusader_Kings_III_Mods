textformatting = {

	format = {
		name = HM_RE_basis_default
		format = "color:{0.5, 0.5, 0.5}"
	}

	format = {
		name = HM_RE_basis_player
		format = "color:{0, 0.5, 1}"
	}
	format = {
		name = HM_RE_basis_ai
		format = "color:{1, 0.5, 0}"
	}
	format = {
		name = HM_RE_basis_all
		format = "color:{1, 1, 1}"
	}

	format = {
		name = HM_RE_basis_enabled
		format = "color:{0.5, 1, 0.5}"
	}
	format = {
		name = HM_RE_basis_disabled
		format = "color:{0.5, 0.5, 0.5}"
	}
	
	format = {
		name = HM_RE_basis_0
		format = "color:{1, 1, 1}"
	}

}
